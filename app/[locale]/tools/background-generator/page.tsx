import { redirect } from 'next/navigation';

export default function BackgroundGeneratorPage() {
  // Redirect to the new AI Background Generator with all professional features
  redirect('/tools/ai-background-generator');
}
